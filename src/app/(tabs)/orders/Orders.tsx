import { Text, View } from "react-native";

const Orders = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>订单</Text>
    </View>
  );
};

export default Orders;
